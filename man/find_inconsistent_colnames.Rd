% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonization_fns.R
\name{find_inconsistent_colnames}
\alias{find_inconsistent_colnames}
\title{A function to identify inconsistent column names across data frames}
\usage{
find_inconsistent_colnames(data_list)
}
\arguments{
\item{data_list}{A list of data frames to check.}
}
\value{
A character vector of column names that are not shared across all data frames.
These are the inconsistent or unique column names that appear in only some of the data frames.
}
\description{
This function checks for consistency in column names across a list of data frames.
It returns the symmetric difference between the shared columns (those present in all data frames)
and the union of all column names (those present in any data frame).
}
\examples{
df1 <- data.frame(a = 1, b = 2)
df2 <- data.frame(a = 3, c = 4)
df_list <- list(df1, df2)
find_inconsistent_colnames(df_list)
# Returns: "b" "c"

}
