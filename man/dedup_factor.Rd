% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonization_fns.R
\name{dedup_factor}
\alias{dedup_factor}
\title{Deduplicate a factor by keeping only its minimum level}
\usage{
dedup_factor(col, by_levels = TRUE)
}
\arguments{
\item{col}{A vector. Must be a factor}

\item{by_levels}{Logical. If TRUE (default), uses the factor's level order
to determine the minimum level. If FALSE, uses lexicographic order.}
}
\value{
A factor vector containing only the observations that match
  the minimum level.
}
\description{
This function filters a factor vector to keep only the elements
that correspond to its minimum level.
}
\examples{
f <- factor(c("B", "A", "C", "B", "A"))
dedup_min_level(f)

f2 <- factor(c("z", "x", "y", "z"))
dedup_min_level(f2, by_levels = FALSE)

}
